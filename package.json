{
  "name": "growth-scholar-mern",
  "version": "1.0.0",
  "private": true,
  "description": "Growth Scholar — MERN port of the static marketing/LMS/creator platform",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently -n server,client -c green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "test": "npm --prefix client run build && npm --prefix server test",
    "test:server": "npm --prefix server test",
    "lint": "npm --prefix server run lint && npm --prefix client run lint",
    "format": "npm --prefix server run format && npm --prefix client run format",
    "format:check": "npm --prefix server run format:check && npm --prefix client run format:check",
    "check": "npm run lint && npm run format:check && npm test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "eslint": "^9.39.5",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.9.0",
    "prettier": "^3.9.6"
  }
}
